import { html } from 'lit';
import { customElement } from 'lit/decorators.js';
import { View } from '../view';

@customElement('datepicker-view')
export class DatePickerView extends View {

  /**
   * Tasks to complete:
   *
   * 1.
   * 1.1. Add a Date Picker to the view
   * 1.2. ???
   *
   * 2.
   * 2.1. ???
   * 2.2. ???
   *
   */
  render() {
    return html`
    `;
  }
}
