package com.example.application.views.datepickerview;

import com.vaadin.flow.component.orderedlayout.VerticalLayout;
import com.vaadin.flow.router.PageTitle;
import com.vaadin.flow.router.Route;
import com.vaadin.flow.router.RouteAlias;

@PageTitle("Date Picker View")
@Route(value = "date-picker-view")
@RouteAlias(value = "")
public class DatePickerView extends VerticalLayout {

    /**
     * Tasks to complete:
     *
     * 1.
     * 1.1. Add a Date Picker to the view
     * 1.2. Set a valid custom reference date using i18n
     *
     * 2.
     * 2.1. Add a Date Picker to the view
     * 2.2. Set an invalid custom reference date using i18n
     *
     * 3.
     * 3.1. Add a Date Picker to the view
     * 3.2. Set a locale to the Date Picker
     * 3.3. Add another Date Picker to the view
     * 3.4. Set a short year custom date format to the Date Picker
     * 3.5. Add another Date Picker to the view
     * 3.6. Set a 4-digit year custom date format to the Date Picker
     * 3.7. Add fields that display the value of each Date Picker
     *
     */
    public DatePickerView() {
    }
}
